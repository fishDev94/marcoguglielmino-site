query performanceCard($locale: String, $discipline: String) {
    performanceCardCollection(limit: 100, locale: $locale, order: [date_DESC], where: { discipline: $discipline }) {
        items {
            ...PerformanceCardData
        }
    }
}